<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('./partials/navbar.ejs') %>
    <% if (gender == 'male') { %>
        <h1><%= gender %></h1>
        <% }else if(gender == 'female') { %>
            <h1><%= gender %></h1>
    
        <% }else{ %>
            <h1><%= gender %></h1>
            <% } %>
     
     
     

    <h1 class="text-danger">Home my home, when shall I see my home</h1>
</body>
</html>